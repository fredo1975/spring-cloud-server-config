server:
  port: 8761

eureka:
  environment: dev
  instance:
    hostname: discovery-server-2
  client:
    register-with-eureka: true
    fetch-registry: false
    serviceUrl:
      defaultZone: http://192.168.1.105:${server.port}/eureka/,http://192.168.1.103:${server.port}/eureka/