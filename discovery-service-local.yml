eureka:
  environment: local
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
      
logging:
  level:
    com:
      netflix:
        eureka: OFF
        discovery: OFF